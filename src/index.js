import React from 'react';
import ReactDOM from 'react-dom/client';
import Generate from './QRCode/Generate';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Generate />
  </React.StrictMode>
);



